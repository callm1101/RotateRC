#include "basic/solve.h"

void rayCasting(ReadMode read_mode, MaskMode mask_mode, File& file);

void rotateRC(int theta_difference, ReadMode read_mode, MaskMode mask_mode, File& file);

void shapeJudge(ReadMode read_mode, MaskMode mask_mode, File& file);
